import React from 'react';

import RegisterToProjectPanel from './RegisterToProjectPanel';

export default {
  title: 'Common/RegisterToProjectPanel',
  component: RegisterToProjectPanel
};

const Template = (args) => <RegisterToProjectPanel {...args} />;

export const Default = Template.bind({});
Default.args = {};
